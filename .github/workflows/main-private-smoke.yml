name: main-private-smoke

on:
  pull_request:
    branches: [ main_private ]

jobs:
  smoke:
    name: Windows runner smoke test
    runs-on: windows-2022
    steps:
      - run: echo "windows-runner-started"
