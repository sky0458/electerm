name: main-private-smoke

on:
  pull_request:
    branches: [ main_private ]

jobs:
  smoke:
    name: Checkout action smoke test
    runs-on: windows-2022
    steps:
      - uses: actions/checkout@v4
      - run: echo "checkout-action-started"
